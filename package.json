{
  "name": "tinysquash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-04-03",
    "test": "node --test",
    "deploy": "wrangler pages deploy public --project-name tinysquash"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
