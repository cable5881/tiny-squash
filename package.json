{
  "name": "tinysquash",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-04-03",
    "build": "node build.js",
    "test": "node --test",
    "deploy": "npm run build && wrangler pages deploy public --project-name tinysquash"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
