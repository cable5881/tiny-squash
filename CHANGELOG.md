# TinySquash 更新日志

记录每次版本更新的功能变更。在每次合入 `main` 分支前更新此文件。

---

## 2026-04-04 (v1.1)

### 新增
- **Cloudflare D1 数据库集成**：存储全量用户信息和访问记录
- **用户角色体系**：支持游客（未登录）、普通用户、管理员三种角色
- **管理后台**：管理员可在首页导航栏进入 `/admin.html`，查看全量用户列表和访问记录，支持分页
- **自动用户注册**：Google 登录时自动写入/更新用户信息到 D1，首次登录自动注册
- **访问记录追踪**：每次登录自动记录时间、IP、User-Agent

### 优化
- **`/api/auth/me`** 接口新增返回 `role` 字段，支持前端角色判断
- **管理员入口**：导航栏仅对管理员显示管理后台图标按钮

---

## 2026-04-04 (v1.0)

### 新增
- **更新日志入口**：页面底部新增"查看最新更新日志"链接

### 优化
- **登录区域重新设计**：登录按钮和已登录状态改为紧凑的导航栏内联样式
- **退出按钮图标化**：退出登录改为小图标按钮
- **图片缩略图约束**：结果表格中的预览图限制为 48x48px 缩略图
- **对比预览区高度限制**：最大高度 480px
- **浏览器缓存策略优化**：静态资源改为 `must-revalidate` + 版本号注入

### 修复
- 控制台 `TypeError: Cannot read properties of null` 报错（id 不匹配）
- 质量值 `parseInt` 解析错误，改为 `parseFloat`
- 质量百分比显示 `0.8%` → `80%`

---

## 2026-04-03

### 初始版本
- 纯前端图片压缩（JPG / PNG / WebP）
- 质量滑块调节、输出格式选择、最大宽度限制
- 压缩前后对比滑块预览
- 批量压缩 & ZIP 打包下载
- Google OAuth 登录
- Cloudflare Pages 部署
